import OrderParamRows from "@site/src/components/paramRowComponents/OrderParamRows"

# GET ./orders/:ORDER_ID

Endpoint to retrieve an order by Order ID.

## Request

<BaseUrlPath>orders/:ORDER_ID</BaseUrlPath>

<RequestParamTable route>

<RequestParamRow
parameter="ORDER_ID"
required
description='A string referring to the order ID. You can pass the system "_id" for the order, or the "orderId" set by the client application.'
/>

</RequestParamTable>

## Response

:::info
Fields marked "(external)" are fields that are set by the client application when posting orders to the restoration dashboard system. These fields can be queried for equality when querying this endpoint.
:::

<ResponseParamTable>

<OrderParamRows populateTrees />

</ResponseParamTable>